/*
  Programa: Hello World em C
*/
#include <stdio.h>   // carregando biblioteca padrão
                     // de entrada e saída.

/*
  main: função principal que, nesse exemplo, não recebe
  parâmetro (void) e retorna inteiro (int).
*/
int main(void)   // main é o identificador da função principal
{                // blocos de código ficam entre chaves.
  printf("Hello World!!");  // função básica de saída

  return 0;   // retornar 0 indica ao sistema operacional
              // que o programa terminou bem.
}

/*
  observe os comentários em mais de uma linhas
*/

// comentário em uma linha
